#include "push_swap.h"
#include "stack.h"

char	*push_swap_hard(t_vars *vars)
{
	t_stack	*a;

	a = vars->a;
	/*
	if (ft_stsize(a) == 3)
		hard_three(vars);
		*/
	return (NULL);
}

char	*hard_three(t_vars *vars)
{
	if (ft_issorted(vars->a))

}
