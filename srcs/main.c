#include "push_swap.h"

int	main(int argc, char **argv)
{
	t_stack	*a;
	t_stack	*b;
	int		size;

	a = parse_argv(argc, argv);
	size = ft_stsize(a);
	b = NULL;
	return (0);
}


/*
int	test(void)
{
	t_stack	*a;
	t_stack	*b;

	a = parse_argv(argc, argv);
	if (a == NULL)
		return (NULL);
	b = NULL;
}
*/
